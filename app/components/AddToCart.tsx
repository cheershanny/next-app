'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button className='btn' onClick={() => alert("Hi")}>Add to Cart</button>
    </div>
  )
}

export default AddToCart